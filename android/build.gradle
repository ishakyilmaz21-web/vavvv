// android/build.gradle  — root project için minimal yapı (Gradle 8 uyumlu)

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    // DİKKAT: BURADA *dependencies { classpath ... }* OLMAYACAK!
    // Flutter/Android Gradle plugin classpath'leri artık burada tanımlanmıyor.
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// İsteğe bağlı: "gradle clean" için yardımcı task
tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
